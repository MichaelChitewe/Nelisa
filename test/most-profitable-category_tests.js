var assert = require('assert');
var purchases = require('../most-profitable-category');

describe('list the most profitable category weeek 1-4',function(){
it('return the most profitable category week 1-4',function(){



  // assert.deepEqual(,purchases.weekPurchasesMap());
  // assert.deepEqual(,purchases.weekPurchasesMap());
  // assert.deepEqual(,purchases.weekPurchasesMap());
  // assert.deepEqual(,purchases.weekPurchasesMap());
})

})
